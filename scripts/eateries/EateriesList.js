import {useEateries} from "./EateryProvider.js"

const eventHub = document.querySelector(".container")
const contentTarget = document.querySelector("#tripDropDown__eatery")

//create single HTML 'card'

// needs to listen to the eventHub

// a function that gets exported so any module can execute it

// the function that renders HTML to the DOM 